pipeline {
   agent any
   stages {
       stage('Build') {
           steps {
               echo "building-project"
           }
       }
       stage('Test') {
           steps {
               echo "testing"
           }
       }
       stage('Deploy') {
           steps {
              echo "deploying"
           }
       }
   }
}